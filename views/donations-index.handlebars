<ul class="list-group">
    {{#each donations}}
        <li class="list-group-item">
        <p><a href="/donations/{{this._id}}">To: {{this.orgsName}}</a></p>
        <p>Amount: {{this.amount}}</p>
        </li>
    {{/each}}
</ul>
